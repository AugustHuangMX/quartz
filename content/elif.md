The `elif` clause is an extension of the [[If-else]] statement in [[Python MOC]]. 

It is just a short for `else if` 

So that, the `else` statement could only appear in the last sentence of the codes.

